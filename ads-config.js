(function () {
  "use strict";

  // Live advertising stays disabled until the AdSense site is approved and
  // the real publisher/ad-unit IDs have been added by the site owner.
  window.FOURCAST_AD_CONFIG = Object.freeze({
    enabled: false,
    client: "",
    slots: Object.freeze({
      home: "",
      gameOver: ""
    })
  });
})();
