# FOURCAST 의사결정 기록

이 문서는 FOURCAST의 기획, 밸런스, UI, 구현 방향과 변경 이력을 날짜순으로 기록한다. 과거 대화의 정확한 메시지별 타임스탬프가 프로젝트 파일에 남아 있지 않은 경우 날짜에 `(추정)`을 표시한다.

## 기록 기준

- 날짜는 한국 시간 기준 `YYYY-MM-DD`로 기록한다.
- 과거 날짜는 확인 가능한 날짜와 추정 날짜를 구분한다.
- 기획만 확정된 내용은 `결정`, 실제 코드에 반영된 내용은 `구현 완료`로 표시한다.
- 구현 완료 항목에는 변경 파일과 실행한 검증을 남긴다.
- 기존 기록은 삭제하거나 덮어쓰지 않는다. 규칙이 바뀌면 새 `정정` 또는 `변경 결정` 항목을 추가한다.
- 같은 주제의 규칙이 충돌하면 최신 날짜의 명시적인 결정을 우선한다.
- 첨부 이미지는 프로젝트에 복사하지 않고 필요한 경우 UI 참고 이미지로만 기록한다.
- 이 문서는 프로젝트 의도와 변경 이력을 보존한다. 실제 동작의 최종 기준은 현재 코드와 최신 결정 항목이다.

## 2026-08-30 (추정) — FOURCAST 초기 기획과 CLASSIC 확정

### 결정

- 모바일 웹 우선, 세로 화면을 기본으로 하고 추후 네이티브 앱 확장을 고려한다.
- 게임은 4개의 세로 라인을 사용하는 무한 생존형 숫자 블록 제거 게임으로 한다.
- 웨이브마다 4개 라인에 숫자 블록 4개를 동시에 생성하며 숫자는 `1~4`에서 균등 랜덤으로 선택한다.
- 블록은 테트리스식 계단이 아니라 DJMAX 노트처럼 시간 기반으로 부드럽게 아래로 이동한다.
- RED LINE에 블록이 닿는 것을 기준으로 게임 오버를 판정하되, 최종 판정은 반응형 블록 높이를 고려한다.
- 각 라인 하단의 버튼으로 전역 `current Next` 숫자를 해당 라인에 입력한다.
- CLASSIC에서는 선택한 라인의 최하단 활성 블록과 current가 일치할 때만 발사체를 생성한다.
- 정답 발사체가 대상 블록에 도달하면 대상 블록이 약 160ms 동안 축소되며 사라지고, 발사체도 소멸한다.
- 오답이면 발사하지 않고 current·preview·점수·블록을 유지한다. 선택한 최하단 블록에는 빨간 외곽선 글로우와 흔들림을 표시한다.
- 빈 라인 입력은 발사하지 않으며, 해당 라인의 `.lane-track` 상단 경계선에만 연핑크 글로우를 표시한다. 별도 사각형 요소와 흔들림은 사용하지 않는다.
- 정답·오답·빈 라인 입력 중에는 버튼을 잠그고, 잠금 상태는 버튼 외곽 회색 하이라이트로 알린다. CLASSIC 발사체는 전역으로 하나만 허용한다.
- 점수는 블록 제거 1회마다 10점으로 한다.

### Next 및 안전성

- current는 전역 Next 1개이며, 첫 current는 첫 번째 visible wave의 최하단 블록 4개에서 중복 제거한 숫자 중 하나로 선택한다.
- preview는 각 라인의 논리적 프론티어 4개를 기준으로 생성한다.
  - 활성 블록이 있으면 해당 라인의 가장 아래 활성 블록을 프론티어로 사용한다.
  - 빈 라인은 가장 가까운 미래 웨이브의 같은 라인 블록을 프론티어로 연결한다.
  - 프론티어의 다음 블록은 같은 라인의 다음 활성 블록 또는 연결된 미래 웨이브에서 가져온다.
- preview 후보를 고를 때 숫자 중복을 제거하지 않고 라인별 슬롯을 그대로 유지한다.
- 프론티어에 current가 없으면 4개 슬롯 전체에서 선택한다.
- current가 있으면 레벨별 `currentLookProbability`에 따라 current 블록의 다음 블록을 보거나, current 슬롯 하나를 랜덤으로 제외한 나머지 3개 슬롯 중에서 선택한다.
- `currentLookProbability`는 Lv.1~30에서 `(level - 1) × 0.02`, Lv.31 이후 `min(0.70, 0.58 + (level - 30) × 0.012)`로 계산한다.
- `safetyCheck`는 Lv.1~10에서 활성화하고, 모든 가능한 current 입력 결과에서 preview 숫자가 이어지는 후보만 허용한다. Lv.11부터는 이 필터를 해제한다.
- 안전성 검사는 `[3, 3, 3, 3]` 다음에 `[1, 1, 1, 1]`이 오는 경우처럼 어느 라인을 제거해도 preview가 이어지는 중복 상황을 안전한 것으로 인정한다.
- `starvationGuard`는 모든 레벨에서 활성화하며, 위험 라인의 후보 슬롯에만 2배 가중치를 적용한다. 후보 숫자는 unique 처리하지 않는다.
- 기아 방지는 current-look 확률 자체를 바꾸지 않고, 확률 분기와 안전성 필터가 끝난 최종 후보 슬롯에만 적용한다.

### 난이도와 시작 배치

- 레벨은 실제 경과 시간 기준 15초마다 상승하며, 레벨 전환은 500ms 동안 보간한다.
- 낙하 속도 절반 조정 이후 `fallDurationSeconds`는 기존 표의 2배가 되었고, 웨이브 간격도 기존 값의 2배가 되었다.
- CLASSIC의 웨이브 간격은 레벨 1의 4.00초에서 시작하고 고레벨 하한은 3.20초로 둔다.
- 레벨 21~30은 이동시간을 레벨마다 0.4초씩 줄이며, Lv.31 이후 이동시간은 다음 공식으로 계산한다.

  `fallDuration = 6.4 + (8.4 - 6.4) × 0.93^(level - 30)`

- CLASSIC 시작 시 두 번째 웨이브는 실제 생성 시작점인 `normalizedY = 0`에 두고, 첫 번째 웨이브는 다음 식으로 계산한 간격만큼 아래에 둔다.

  `initialGap = waveIntervalSeconds × RED_LINE_Y / fallDurationSeconds`

- 이후 생성되는 웨이브도 `normalizedY = 0`에서 시작해 웨이브 사이의 세로 간격을 일정하게 유지한다.
- 정규화된 위치를 사용해 기기 크기와 무관하게 동일한 낙하시간을 유지한다.

### 반응형 및 시각 디자인

- `100dvh`, safe-area inset, 가용 너비 기반 라인 계산을 사용한다.
- 4개 라인은 동일한 비율로 배치하고, 넓은 화면이나 태블릿에서는 최대 너비를 제한한다.
- 320px급 소형 화면에서도 수평 스크롤과 UI 잘림이 없어야 한다.
- 폰트는 Pretendard Variable을 기본으로 하며 숫자·제목은 굵게, 숫자는 tabular number와 짧은 line-height를 사용한다.
- 폰트 크기는 `clamp()`와 요소 폭을 이용해 화면 크기에 따라 조절한다.
- 숫자 타일은 따뜻한 크림 배경과 파스텔 블루·민트·앰버·퍼플을 사용하되, 색상만으로 숫자를 구분하지 않는다.
- 숫자와 red line이 우선적으로 읽혀야 하며 강한 글로우는 RED LINE과 피드백에 한정한다.

### UI와 다국어

- 게임 제목은 `FOURCAST`로 한다.
- 시작 화면의 보조 문구 `Match the fall. Stay ahead.`와 `NUMBER SURVIVAL`은 제거한다.
- 도움말 본문은 다음 짧은 한 문장으로 한다.

  `내려오는 숫자 블록과 일치하는 버튼을 클릭하여 빨간 선에 닿기 전에 블록을 제거하세요.`

- 도움말에는 `HOW TO PLAY`만 접근성 제목으로 유지하고, `FOURCAST 플레이 방법` 제목은 제거한다.
- 플레이 화면의 `RED LINE` 텍스트와 라인 버튼의 시각적 숫자·`LINE` 문구를 제거한다. 버튼은 시각적으로 비워 두고 접근성 라벨은 유지한다.
- 설정 버튼은 시작 화면에서 도움말 버튼 오른쪽에 둔다. 플레이 화면 음표 버튼의 오른쪽 기준선은 설정 버튼과 맞춘다.
- 설정 모달에서는 `ko`·`en` 언어, BGM 켜기/끄기, BGM 음량을 관리한다.
- 설정은 변경 즉시 적용하며 언어·BGM 활성 상태·음량·최고점수를 로컬에 저장한다.
- 언어 초기값은 저장값을 우선하고, 저장값이 없으면 브라우저 언어가 한국어일 때 `ko`, 그 외에는 `en`으로 한다.
- 게임 오버에는 설정·음표 버튼을 두지 않고 `메인 메뉴`와 `다시 시작`만 제공한다. 모드별 최고점수를 표시한다.

### BGM

- BGM은 Web Audio 단일 `AudioBufferSourceNode`의 `loop=true`를 공식 재생 경로로 사용한다.
- `file://` 환경에서는 Web Audio 준비를 지원하지 않고 `blocked` 상태로 둔다. HTML Audio fallback이나 교차 페이드는 사용하지 않는다.
- 공식 BGM 파일은 `audio/fourcast-bgm-v2.wav`이며, HTTP/HTTPS 환경에서 다운로드·디코드한 뒤 사용한다.
- `loopStart = 0`, `loopEnd = buffer.length / buffer.sampleRate`로 실제 디코드 버퍼 전체를 반복한다.
- Start·Restart 후 BGM 준비 중에는 화면 중앙에 `준비 중…` 또는 `PREPARING…`을 표시하고 큰 카운트다운 숫자는 숨긴다. 준비가 끝나거나 최대 2초가 지나면 카운트다운을 시작한다.
- 준비 중 문구의 시머는 텍스트에만 적용하며, 흰색·밝은 하이라이트가 왼쪽에서 오른쪽으로 지나간다. `prefers-reduced-motion`에서는 애니메이션을 끈다.
- 최초 `GO` 재생은 저장된 목표 음량으로 시작하고, 자동 일시정지 복귀와 음표 버튼 재생에는 600ms 페이드인을 적용한다.
- BGM이 재생 중단·게임 오버·메인 메뉴로 전환되면 source와 페이드를 정리하고 다음 게임에서 새 source를 만든다.

### 구현 상태

- 구현 완료: `app.js`, `index.html`, `styles.css`, `README.md`, `audio/fourcast-bgm-v2.wav`에 관련 규칙과 UI를 반영했다.
- 구현 완료: Web Audio 진단용 `getState()` 필드, 다국어 설정, BGM 페이드, 반응형 레이아웃, 빈 라인 상단 경계선 글로우를 반영했다.

### 검증

- `node --check app.js` 통과.
- `git diff --check` 통과.
- HTTP 로컬 서버에서 BGM v2 요청, Web Audio backend, 반응형 뷰포트, Stack Rush 주요 동작을 확인했다.
- 320×568, 390×844, 768×1024에서 수평 스크롤과 주요 UI 잘림을 확인했다.

## 2026-08-30 (추정) — STACK RUSH 모드 확정

### 결정

- 기존 모드는 `classic`, 새 모드는 `stack-rush`로 구분한다.
- STACK RUSH는 클릭 후 입력 제한을 두지 않아 빠른 연속 입력을 허용한다.
- 버튼 클릭은 발사체를 항상 생성한다. current와 클릭 라인의 최하단 블록 숫자가 일치하면 정답, 다르면 발사 숫자 타일이 해당 라인에 패널티 블록으로 쌓인다.
- 발사체는 실제 숫자 블록과 같은 크기·색상·숫자를 가진 타일로 만들며, 이동시간은 CLASSIC의 약 190ms보다 빠른 약 127ms로 한다.
- 발사체는 전역으로 1개만 이동하지만, 클릭한 라인 번호는 최대 4개까지 큐에 저장한다. 큐 초과 입력은 무시한다.
- 큐에는 라인 번호만 저장하고 실제 발사 순간의 current를 발사체 숫자로 복사한다.
- 정답 충돌에서는 발사 타일과 대상 블록이 약 160ms 동안 함께 축소되어 사라진다.
- 오답 충돌에서는 발사 타일이 정착하고, 오답으로 쌓인 블록과 기존 최하단 블록에 빨간 글로우·흔들림을 표시한다. 새로 정착한 패널티 블록은 빈 라인에서도 대기 스택으로 유지한다.
- STACK RUSH에서는 버튼을 잠그지 않고 약 80~100ms의 짧은 눌림 효과만 표시한다.
- 발사 시작 순간 이전 current를 발사체에 복사한 뒤 current를 preview로 즉시 승격하고, 예측된 정답 제거·오답 적층·빈 라인 패널티를 기준으로 새 preview를 생성한다.
- 실제 충돌 시에는 Next를 다시 갱신하지 않으며, 발사 시작마다 정확히 한 번만 갱신한다.
- 발사 중인 블록은 RED LINE 판정에서 제외한다. 정답 대상 블록과 적층·대기 패널티 블록은 판정 대상이다.
- 발사 중인 블록이 RED LINE을 넘어도 게임 오버가 아니며, 잘못 쌓인 패널티 블록이 RED LINE을 넘으면 게임 오버다.
- STACK RUSH의 낙하시간은 CLASSIC의 1.5배로 계산한다.

  - Lv.1: 30초
  - Lv.10: 24.6초
  - Lv.20: 18.6초
  - Lv.21: 18초
  - Lv.30: 12.6초
  - Lv.31 이후: `9.6 + (12.6 - 9.6) × 0.93^(level - 30)`

- 웨이브 간격은 고정 테이블 대신 실제 블록 피치 `P`를 사용해 계산한다.

  `waveInterval = P × fallDuration / RED_LINE_Y`

- STACK RUSH 시작 시 3개 웨이브를 `2P`, `P`, `0` 위치에 배치하고 미래 웨이브 큐는 2개를 유지한다.
- current·preview·safetyCheck·starvationGuard의 기존 프론티어 규칙은 STACK RUSH에도 적용한다. 패널티 블록과 빈 라인의 대기 패널티는 즉시 프론티어로 계산한다.
- 점수는 정답 충돌 1회당 10점이며 CLASSIC과 STACK RUSH의 최고점수는 분리한다.

### UI

- 시작 화면에 CLASSIC과 STACK RUSH 두 개의 컴팩트한 세로형 모드 선택 카드를 배치한다.
- 카드 전체는 접근 가능한 선택 버튼 또는 라디오 그룹으로 사용하고, 최초 방문 시 CLASSIC을 선택한다. 이후 마지막 선택 모드를 복원한다.
- 카드에는 모드별 설명·CSS 미니 아이콘·해당 모드 최고점수를 표시한다.
- 선택된 카드에는 민트색 테두리와 체크 표시를 사용한다.
- 플레이 화면 Score 영역에는 현재 모드의 작은 배지를 표시한다. CLASSIC은 민트, STACK RUSH는 앰버 계열이다.
- 게임 오버 화면에는 현재 모드 배지·점수·레벨·해당 모드 `BEST`를 표시하며, `RESTART`는 같은 모드로, `MAIN MENU`는 마지막 선택 모드를 유지한 채 시작 화면으로 이동한다.
- 모드 카드 높이는 이후 화면이 비어 보이는 문제를 해결하기 위해 약 2배로 확대하고, 타이틀과 시작 콘텐츠 그룹은 위쪽으로 이동한다.

### 구현 상태

- 구현 완료: `app.js`, `index.html`, `styles.css`에 모드 상태·입력 큐·예측 Next·패널티 적층·모드별 점수와 UI를 반영했다.
- 구현 완료: `README.md`의 `getDifficulty(level, mode)` 및 모드 검증 안내를 갱신했다.

### 검증

- STACK RUSH 발사 시작 즉시 current·preview가 갱신되고 발사체에는 이전 current가 표시되는지 확인했다.
- 연속 입력 큐와 최대 4개 제한, 정답 제거·오답 적층 경로를 확인했다.
- 모드 카드 확대 후 320×568, 390×844, 768×1024에서 잘림과 수평 스크롤을 확인했다.
- `node --check app.js` 통과.
- `git diff --check` 통과.

## 2026-08-31 — 모바일 확대 차단

### 결정

- 게임 전체에서 모바일 브라우저의 더블탭 확대와 두 손가락 핀치 확대를 차단한다.
- viewport에 `maximum-scale=1`, `user-scalable=no`, `viewport-fit=cover`를 적용한다.
- 전체 앱 영역에는 `touch-action: manipulation`을 적용한다.
- `touch-action: none`과 별도 더블클릭 이벤트 차단 코드는 사용하지 않는다. 슬라이더와 네이티브 컨트롤 조작을 유지하기 위함이다.

### 구현 상태

- 구현 완료: `index.html` viewport 설정과 `styles.css` 앱 영역 터치 정책에 반영했다.
- 게임 규칙, 입력 큐, BGM, 언어, 공개 API는 변경하지 않았다.

### 검증

- 빠른 버튼 연속 터치, 라인·빈 공간 더블탭, 도움말·설정 모달의 확대 차단을 확인했다.
- 모드 카드, 라인 버튼, 설정 드롭다운, BGM 토글·슬라이더의 기존 조작이 유지되는지 확인했다.
- `node --check app.js` 통과.
- `git diff --check` 통과.

## 2026-08-31 — 의사결정 기록 문서 도입

### 결정

- 프로젝트 루트의 [DECISIONS.md](./DECISIONS.md)에 앞으로의 기획·구현·검증 결정을 누적 기록한다.
- 기능 구현이 끝날 때마다 날짜순으로 새 항목을 문서 하단에 추가한다.
- 구현 기록에는 변경 파일, 핵심 동작, 실행한 검증을 함께 남긴다.
- 과거 결정이 변경되면 과거 항목을 지우지 않고 새 `정정` 또는 `변경 결정` 항목으로 남긴다.

### 구현 상태

- 구현 완료: 이 문서를 프로젝트 루트에 생성했다.
- 이후 구현부터 동일한 기록 원칙을 적용한다.

### 검증

- 문서가 프로젝트 루트에 존재하는지 확인한다.
- 초기 주요 결정, 추정 날짜 표시, 변경 이력 원칙, 재사용 가능한 기록 형식이 포함되어 있는지 확인한다.

## 향후 기록 템플릿

새로운 결정이나 구현을 기록할 때 다음 형식을 사용한다.

```md
## YYYY-MM-DD — 제목

### 결정

- 무엇을 결정했는지 기록한다.

### 결정 이유

- 왜 이 결정을 했는지 기록한다.

### 구현 상태

- 구현 완료 / 예정 / 변경 없음
- 변경 파일: `path/to/file`

### 검증

- 실행한 테스트와 확인 결과를 기록한다.
```

## 2026-08-31 — STACK RUSH 빈 라인 패널티·Jelly Wobble 수정

### 결정

- 이번 변경 범위는 STACK RUSH로 한정하고 CLASSIC의 규칙·연출·코드는 변경하지 않는다.
- 신규 웨이브는 항상 `normalizedY = 0`에서 생성한다.
- 빈 라인에 주차된 패널티 스택이 있으면 각 패널티를 블록 피치 `P`만큼 아래로 이동하고 `parked`를 해제한다.
- 기존 패널티가 `0, P, 2P`에 있으면 신규 웨이브는 `0`, 패널티는 `P, 2P, 3P`에 배치한다.
- 위치 분리는 생성 순간 즉시 적용해 신규 웨이브와 패널티가 겹치는 상태를 만들지 않는다.
- 빈 라인 발사와 웨이브 생성이 겹치면, 이미 생성된 웨이브를 기준으로 패널티를 `targetY + P`에 배치해 두 블록이 겹치지 않게 한다.
- 오답 적층에서는 기존 최하단 블록과 새 패널티 블록에만 충돌 연출을 적용한다.
- 신규 웨이브 유입에서는 신규 웨이브와 이동된 패널티 중 최상단 블록에만 충돌 연출을 적용한다.
- Jelly Wobble은 충돌 방향을 `from-top`과 `from-bottom`으로 구분하고, 압축·반발·회전·감쇠를 포함한 180ms 애니메이션으로 표현한다.
- Jelly Wobble은 실제 `normalizedY`, 낙하 속도, RED LINE 판정, Next, 입력 큐에 영향을 주지 않는다.
- STACK RUSH 오답에서는 기존 빨간 글로우·가로 흔들림을 사용하지 않고 Jelly Wobble만 표시한다.
- 접촉하지 않은 단독 빈 라인 패널티에는 Jelly Wobble을 표시하지 않는다.

### 구현 상태

- 구현 완료: `app.js`에서 parked 패널티 재배치, 접촉 쌍, 충돌 방향 상태를 반영했다.
- 구현 완료: `styles.css`에서 실제 타일 크기 기반 Jelly Wobble keyframe을 추가했다.
- 구현 완료: 기존 CLASSIC 오답 피드백 코드는 유지했다.

### 검증

- `node --check app.js` 통과.
- `git diff --check` 통과.
- 로컬 HTTP 화면에서 STACK RUSH 오답 시 두 접촉 블록에만 `stack-jelly-wobble`이 적용되는 것을 확인했다.
- 같은 상황에서 `is-wrong`와 `block-shake`가 적용되지 않는 것을 확인했다.
- 신규 웨이브 생성 코드가 시작점 `0`과 패널티 스택 `+P` 이동을 사용하고, 별도의 `-P` 생성 보정이 제거된 것을 확인했다.
- 빈 라인 발사 직후 웨이브가 먼저 생성되는 경로도 현재 최하단 블록의 `targetY + P`를 사용하도록 확인했다.
- 320×568, 390×844, 768×1024 반응형 레이아웃에 영향을 주는 고정 크기 변경이 없는 것을 확인했다.

## 2026-09-06 — 블록 크기 비율 기반 웨이브 간격

### 결정

- 기존 고정 `2px` 의도는 폐기하고 웨이브 외곽 간격을 실제 블록 높이의 `3%`(`waveGapRatio = 0.03`)로 계산한다.
- 블록 높이와 웨이브 피치는 확대·축소되지 않은 `.lane-blocks` 영역의 실수형 크기를 기준으로 한다. 웨이브 피치는 `blockHeight × 1.03`이다.
- 스테이지 프로필에는 비율을 저장하고, Lab에는 현재 화면에서 환산한 중심 간격·외곽 간격·픽셀 목표를 함께 표시한다.
- 새 웨이브 위치는 타이머 오차 보정값이 아니라 현재 최상단 웨이브에서 정확히 한 피치 위로 계산한다.
- 새 웨이브 타일은 진입 순간부터 100% 크기를 유지하고, 기존 160ms 확대 효과는 투명도·밝기 변화로 대체한다. 충돌 젤리·오답 흔들림·제거 축소는 유지한다.
- 웨이브가 들어올 때 새 웨이브 아래의 패널티와 모든 기존 블록을 빈 슬롯 없이 순서대로 한 피치씩 밀어 겹침을 방지한다.
- 모바일 주소창·회전으로 레이아웃 높이가 바뀌면 기존 블록 중심을 먼저 보존한 뒤 새 블록 높이와 3% 비율로 재계산한다.

### 구현 상태

- 구현 완료: `app.js`, `lab/index.html`, `lab/lab.js`, `lab/bootstrap.js`, `README.md`에 반영했다.
- 구현 완료: Classic 모드는 삭제된 상태를 유지하고, Lab 코드는 별도 산출물에서만 포함한다.

### 검증

- `node --check app.js`, `node --check lab/lab.js`, `node --check lab/bootstrap.js` 통과.
- `node scripts/build.mjs`, `node scripts/verify-build.mjs` 통과.
- Lab 05에서 3% 대열과 패널티 리플로우·최대 겹침을 확인하고, Lab 06에서 240ms 프레임 측정으로 진입 외곽 크기와 간격 흔들림을 확인한다.

## 2026-09-07 — Stack Rush 패널티 재배치·완벽 판정·피드백 복원

### 결정

- 신규 웨이브는 현재 최상단 웨이브보다 정확히 한 피치 위에 고정한다. 주차된 패널티는 모두 신규 웨이브 아래로 모으고, 기존 하위 블록까지 순서대로 한 피치씩 연쇄 이동한다. 재합류한 패널티는 `parked = false`로 바꿔 동일한 낙하 속도로 움직인다.
- 오답 충돌은 기존 블록과 새 패널티 양쪽에 실제 타일 폭의 5%(2~5px 제한) 이동량을 사용하는 180ms 방향성 Jelly Wobble을 적용한다. 빈 라인 패널티는 새 블록 하나에만 같은 젤리를 적용한다. 오답 흔들림과 젤리 상태는 별도 클래스로 유지한다.
- 신규 웨이브 진입은 논리적 중심과 3% 피치를 유지하면서 160ms 동안 `scale 0.28 → 1`로 확대한다. 확대·젤리·제거 변환은 기준 블록 크기 측정에 포함하지 않는다.
- 두 Next 연결 잔광과 프레임은 제거한다. Stage 11부터 안전 체크 해제 신호는 preview 토큰의 300ms 맥동 3회로 표시하며, 맥동 중 preview 숫자가 바뀌어도 애니메이션을 취소하지 않는다.
- 현재 Next와 일치하는 실제 라인 중 다음 가시 블록이 preview와 같고, 제거 뒤 preview를 놓을 실제 라인이 정확히 하나인 경로가 하나일 때만 완벽 기회를 만든다. 첫 라인을 맞힌 뒤 같은 라인의 승격 preview를 바로 맞히면 반복 가능한 완벽으로 확정한다. 다른 입력·오답·빈 라인·스테이지 전환·대상 소멸은 경로를 취소한다.
- 점수는 콤보 1에서 10점, 콤보 2~10에서 11점, 콤보 11~20에서 12점, 콤보 21부터 `12 + floor((combo - 11) / 10)`점으로 계산한다. 완벽 두 번째 제거에는 50점을 추가하고 `완벽`/`PERFECT`를 성공·마일스톤보다 우선 표시한다.

### 구현 상태

- 구현 완료: `app.js`, `styles.css`, `lab/index.html`, `lab/lab.js`, `lab/bootstrap.js`, `scripts/verify-build.mjs`, `README.md`.
- 구현 완료: Classic 삭제와 프로덕션/Lab 분리 상태를 유지했다. 첨부 이미지는 참고만 하고 저장소에 복사하지 않았다.
- Lab 05는 라인 끝(`getGameOverY()` 직전)에 주차한 패널티 두 개를 사용해 Y 좌표가 깊어도 신규 웨이브 바로 아래로 우선 재배치되는 경로를 고정 검증한다.

### 검증

- `node --check app.js`, `node --check lab/lab.js`, `node --check lab/bootstrap.js` 통과.
- `node scripts/build.mjs`, `node scripts/verify-build.mjs` 통과.
- Lab 02는 유일 완벽 경로·다중 preview 배치 무효 경로와 콤보 점수를, Lab 04는 preview 3회 맥동과 숫자 변경 유지를, Lab 05·06은 패널티 연쇄 이동·3% 간격·240ms 확대를 확인한다.
